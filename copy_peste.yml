environment: "development"

development:
  modules:
      graphics:
          default: "console_mode/console_mode.rb"
  ports_tree_path: "./"

production:
  modules:
      graphics:
          default: "console_mode/console_mode.rb"
  ports_tree_path: "/"
