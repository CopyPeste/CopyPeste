environment: "development"
development:
  modules:
      graphics:
          default: "console_mode"
  ports_tree_path: "./"

production:
  modules:
      graphics:
          default: "console_mode"
  ports_tree_path: "/usr/ports/"
